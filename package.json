{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development lerna exec --parallel npm run dev",
    "build": "lerna exec --parallel npm run build"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
