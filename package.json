{
  "name": "root",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "graphql",
    "external"
  ],
  "scripts": {
    "dev": "NODE_ENV=development lerna exec  --ignore @marblez/external --parallel npm run dev",
    "build": "lerna exec --parallel npm run build",
    "types": "lerna exec --parallel npx typesync && yarn",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "typesync": "^0.6.1"
  }
}
